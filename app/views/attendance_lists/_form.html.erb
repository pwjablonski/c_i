<%= form_for(@attendance_list) do |f| %>
  <% if @attendance_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@attendance_list.errors.count, "error") %> prohibited this attendance_list from being saved:</h2>

      <ul>
      <% @attendance_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    
    <%= f.hidden_field :classroom_id, value: @classroom.id%>
  
  </div>
  <div class="field">
    <%= f.label :date %><br>
    <%= f.date_select :date %>
  </div>
    <%= f.fields_for :attendance_data do |att| %>
    
  <div class="field">
     <%= att.hidden_field :enrollment_id, value: att.object.enrollment.id%>
  </div>  
      
  <div class="field">
    <%= f.label att.object.enrollment.student.first_name %> <br>
    <%= att.check_box :present %>
  </div>    

    <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
